{
	"name": "parking-alert",
	"scripts": {
		"start": "turbo run start",
		"dev": "turbo run dev --parallel",
		"build": "turbo run build",
		"deploy": "turbo run deploy",
		"format": "prettier --write '*.{json,js}' '**/*.{ts,tsx,js,jsx,md,yml}'",
		"lint": "turbo run lint",
		"lint:fix": "turbo run lint:fix"
	},
	"private": true,
	"engines": {
		"npm": "Please use pnpm instead! run `corepack enable` to enable it, see more: https://nodejs.org/api/corepack.html",
		"yarn": "Please use pnpm instead! run `corepack enable` to enable it, see more: https://nodejs.org/api/corepack.html",
		"node": ">=18.17.0",
		"pnpm": ">=8.0.0"
	},
	"packageManager": "pnpm@8.7.4",
	"devDependencies": {
		"eslint-config-base": "workspace:*",
		"turbo": "1.10.13",
		"prettier": "3.0.2"
	},
	"volta": {
		"node": "18.17.1"
	}
}
